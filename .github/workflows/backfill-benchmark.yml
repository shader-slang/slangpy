name: backfill-benchmark
run-name: "backfill-benchmark: ${{ inputs.target_sha }}"

on:
  workflow_dispatch:
    inputs:
      target_sha:
        description: "Historical main commit to benchmark"
        required: true
        type: string

permissions:
  # This job builds and runs arbitrary historical revisions on a persistent
  # self-hosted runner, so it is given nothing beyond reading the repository.
  # It publishes no checks and requests no OIDC token; results go to BenchView
  # over its own API key.
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.runs-on }}
    # One commit takes minutes, so the default limit is already generous. It is
    # stated anyway so a hung build is reclaimed rather than holding a scarce
    # perf runner for six hours.
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        os: [windows, linux]
        config: [Release]
        python: ["3.10"]
        include:
          - { os: windows, platform: x86_64, compiler: msvc, config: Release, flags: "benchmark", runs-on: { labels: [Windows, X64, nvrgfx-perf-kernelvm-bridge] } }
          - { os: linux, platform: x86_64, compiler: gcc, config: Release, flags: "benchmark", runs-on: { labels: [Linux, X64, nvrgfx-perf-kernelvm-bridge] } }

    env:
      CI_OS: ${{ matrix.os }}
      CI_PLATFORM: ${{ matrix.platform }}
      CI_COMPILER: ${{ matrix.compiler }}
      CI_CONFIG: ${{ matrix.config }}
      CI_PYTHON: ${{ matrix.python }}
      CI_FLAGS: ${{ matrix.flags }}
      BENCHVIEW_API_URL: ${{ vars.BENCHVIEW_API_URL }}
      BENCHVIEW_API_KEY: ${{ secrets.BENCHVIEW_API_KEY }}
      BENCHVIEW_BENCHMARK_BRANCH: main

    steps:
      # The harness checkout supplies the driver, the files overlaid onto every
      # historical tree, and the vcpkg revision known to still bootstrap.
      # persist-credentials is off because the historical code this job builds and
      # runs would otherwise be able to read the job token out of .git/config. The
      # clone below uses the public HTTPS URL and needs no credentials.
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Setup Python ${{ matrix.python }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}

      - name: Setup MSVC
        uses: step-security/msvc-dev-cmd@v1

      - name: Setup CMake/Ninja
        uses: lukka/get-cmake@latest

      # PyTorch is pinned, so it does not vary with the target commit.
      - name: Setup PyTorch environment
        run: |
          python -m pip install torch==2.8.0 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
          python -m pip install pytest-github-actions-annotate-failures

      # The driver owns the whole clone lifecycle: it removes any clone left behind
      # by an earlier run, makes a fresh one, and removes it again whatever the
      # outcome. Keeping that in Python rather than in two shells means it is written
      # once for both runners, and the deletion target is one the script derived
      # rather than one handed to it, so it needs no guards against deleting the
      # wrong directory.
      #
      # The sha is handed over in the environment rather than on the command line
      # so that this one step is written once for both pwsh and bash.
      - name: Benchmark historical commit
        env:
          BACKFILL_TARGET_SHA: ${{ inputs.target_sha }}
        run: >
          python tools/backfill_commit.py
          --work-dir "${{ runner.temp }}/slangpy-backfill"
          --repository-url "https://github.com/${{ github.repository }}.git"
          --run-id "${{ github.run_id }}"
          --api-url "${{ env.BENCHVIEW_API_URL }}"
